{
  "name": "Noir Lab",
  "build": {
    "dockerfile": "Dockerfile"
  },
  "customizations": {
    "vscode": {
      "extensions": [
        "noir-lang.vscode-noir"
      ],
      "settings": {
        "terminal.integrated.defaultProfile.linux": "bash"
      }
    }
  },
  "postCreateCommand": "bb --version | grep 0.82.2 || echo 'Warning: bb version is not 0.82.2'; sh /workspaces/CS595Lab/hw5/gen_toml/init.sh"
}

